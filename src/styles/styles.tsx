import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textCard: {
    color: "#282727",
    fontFamily: "serif",
  },
  textCardTitle: {
    color: "#282727",
    fontFamily: "serif",
    fontSize: 20,
    fontWeight: "bold",
  },
});
